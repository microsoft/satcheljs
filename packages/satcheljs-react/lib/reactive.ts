import {observer} from 'mobx-react';
import * as React from 'react';
import {SelectorFunction} from 'satcheljs';
import

export default function reactive(selector: SelectorFunction) {
    return function(constructor: Function) {

    }
}